@page "/"

@using InitialEnterprise.BlazorFrontend.Shared
@using Microsoft.AspNetCore.Components.Web
@using InitialEnterprise.BlazorFrontend.Services


@inject NavigationManager NavigationManager
@inject IAuthenticationService authenticationService


<h1>Hello, world!</h1>

Welcome to your new app.

@*
    <AuthorizeView>
        <h1>Hello, @context.User.Identity.Name!</h1>
        <p>You can only see this content if you're authenticated.</p>
    </AuthorizeView>

    <AuthorizeView Policy="UserRead">
        <p>You can only see this if you satisfy the "UserRead" policy.</p>
    </AuthorizeView>

        <AuthorizeView Policy="User-Read">
            <p>You can only see this if you satisfy the "User-Read" policy.</p>
        </AuthorizeView>
*@


@code{
    [CascadingParameter]
    private Task<AuthenticationState> authenticationStateTask { get; set; }
}